@echo off

cd "%cache%"

del "%export%"

echo ================================= > %logpath%
echo Advanced ETS2 Packer >> %logpath%
echo %date% %time% >> %logpath%
echo ================================= >> %logpath%

"%7z%" a -tzip "%export%" * >> %logpath%
"%7z%" a -tzip -mx0 "%export%" *.ogg -r >> %logpath%
"%7z%" a -tzip -mx0 "%export%" *.bank -r >> %logpath%